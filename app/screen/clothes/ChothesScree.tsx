import { StyleSheet, Text, View } from "react-native";

export const ClothesScreen = () => {
  return (
    <View>
      <Text>Oke clothes</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  mainBody: {
    flexDirection: "row",
    justifyContent: "center",
  },
});
